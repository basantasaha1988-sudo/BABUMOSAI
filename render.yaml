services:
  - type: web
    name: babumosai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python launcher.py
    envVars:
      - key: PORT
        value: 8501
